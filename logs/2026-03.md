
## 2026-03-19 #note

In Python, `__missing__` on a `dict` subclass is called when a key isn't found.

## 2026-03-19 #note

In Python, `__missing__` on a `dict` subclass is called when a key isn't found.
